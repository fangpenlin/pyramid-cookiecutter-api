machine:
  python:
    version: 3.5.2
dependencies:
  cache_directories:
    - /home/ubuntu/.cache/pip/
  override:
    - 'pip install -U pip'
    - 'pip install wheel'
    - 'pip install cookiecutter'

test:
  override:
    - 'cookiecutter .'
